<!--Form for unfollowing a user. Finds the user's relationship by the show page's user's id. It then call the html method, delete-->
<!--remote allows the delete action to send a request to the server without leaving the page-->
<%= form_for(current_user.relationships.find_by(followed_id: @user.id), html: { method: :delete }, remote: true) do |f| %>
	<%= f.submit "Unfollow", class: "btn btn-large" %>
<% end %>