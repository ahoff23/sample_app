<!--Gravatar linking to user show page-->
<%= link_to gravatar_for(current_user, size:52), current_user %>

<!--Header containing the user's name-->
<h1>
	<%= current_user.name %>
</h1>
<!--span contianing a link to the user's profile-->
<span>
	<%= link_to "View my profile", current_user %>
</span>
<!--Display a count of the users microposts-->
<span>
	<%= pluralize(current_user.microposts.count, "micropost") %>
</span>